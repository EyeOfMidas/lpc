NAME
	&& - logical and

SYNTAX
	mixed a,b,c;
	c = a && b;


DESCRIPTION
	If a is true (not equal to 0) then b is returned. Otherwise 0 will
	be returned. Note that if a is 0 then the statement b will never
	be executed.

SEE ALSO
	and, logical_or, xor


