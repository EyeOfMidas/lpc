NAME
	call_out_info - return an array with pending call_outs
SYNTAX
	mixed *call_out_info();

DESCRIPTION
	Get information about all pending call outs. An array is returned,
	where every item in the array consists at least 3 elements:

	The time left
	The this_player() for this call.
	The function
	The arguments

SEE ALSO
	 call_out, remove_call_out.
