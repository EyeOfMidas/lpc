NAME
	dump_prog_table_data - dump some data about the program hash

SYNTAX
	string dump_prog_table_data(int flavour);

DESCRIPTION
	Returns a string containing readable info about the program hash table.
	The information comes in two flavours, depending on weather the
	argument is 0 or 1.

SEE ALSO
	dump_malloc_data, dump_string_data, dump_opt_status
	dump_heart_beat_data, dump_index_table_data
